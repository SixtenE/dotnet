name: Deploy to test (Macbook Docker)

on:
  push:
    branches: [test]

jobs:
  deploy:
    runs-on: [self-hosted, mac-test]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t dotnet:test .

      - name: Stop/remove old container (if any)
        run: |
          docker rm -f dotnet-test || true

      - name: Run new container
        run: |
          docker run -d \
            --name dotnet-test \
            -p 5290:5290 \
            --restart unless-stopped \
            dotnet:test

      - name: Smoke test
        run: |
          for i in {1..30}; do
            if curl -fsS http://localhost:5290/health >/dev/null; then
              echo "OK"
              exit 0
            fi
            sleep 2
          done
          echo "Health check failed"
          docker logs dotnet-test || true
          exit 1
